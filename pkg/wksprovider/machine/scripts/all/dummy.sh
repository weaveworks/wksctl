# This file exists so that the Makefile will find at least one .sh file for machine scripts.
# It's possible we will want to add new scripts later so the script gathering infrastructure is being left in place.

echo 'A stand-in for builds. If we add any new scripts, this will be unnecessary'

